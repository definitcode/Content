[oplocu,loc_328]
def_obj $last_use = last_useitem;
if ($last_use ! spade){
    mes("Nothing interesting happens..");
} else {
    mes("You use the spade to dig a hole.");
    ~graverobbing(loc_coord);
    loc_change(loc_329, 100);
}


[proc,graverobbing](coord $loc_coord)
def_int $rand = random(128);

switch_int($rand){
    case 12 : {
        npc_add($loc_coord,skeleton_unagressive, 500);
        npc_setmode(opplayer2);
        npc_delay(1);

    }
    case 120 : {
        npc_add($loc_coord,skeleton_unarmed, 500);
        npc_say("Raarrghh!!!!!!");
        npc_setmode(opplayer2);
        npc_delay(1);
    }
}
if ($rand < 119){
    npc_add($loc_coord,giantskeleton, 500);
    npc_say("Aarrghh!");
    npc_setmode(opplayer2);
    npc_delay(1);
} else if ($rand > 120){
    npc_add($loc_coord,giantskeleton, 500);
    npc_say("Braerrggh!!!!!");
    npc_setmode(opplayer2);
    npc_delay(1);
}

[ai_queue1,skeleton_armed] ~npc_default_retaliate_ap;
[ai_queue1,skeleton_unagressive] ~npc_default_retaliate_ap;
[ai_queue1,giantskeleton] ~npc_default_retaliate_ap;
[ai_queue1,skeleton_unarmed] ~npc_default_retaliate_ap;